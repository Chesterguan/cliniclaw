# Rust build artifacts
/target/
**/*.rs.bk

# Cargo lock for applications (keep for binaries, omit for libraries)
# Cargo.lock

# Environment and secrets
.env
.env.*
!.env.example
web/.env*
!web/.env.example
*.pem
*.key
*.p12

# IDE and editor files
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store

# SQLite databases (dev artifacts)
*.sqlite
*.sqlite3
*.db

# Log files
*.log
logs/

# Coverage and profiling
lcov.info
*.profraw
tarpaulin-report.html

# Generated docs (run `cargo doc` to regenerate)
/doc/

# Web frontend build artifacts
web/.next/
web/node_modules/

# Demo recordings (keep .gif for README, skip large video)
demo.mp4

# Test fixtures with real PHI (never commit)
tests/fixtures/real/
linkedin-posts.md
*.png
# …but report chart assets are documentation and ARE tracked:
!docs/reports/assets/*.png

# API keys — NEVER commit. Write real keys into secrets.env locally.
secrets.env
.env
.env.local

# Generated proposal exports — PROPOSAL.md is the source of truth.
# Built .docx/.html are published as GitHub Release assets, never committed
# (git can't meaningfully diff binaries). build_share.py stays tracked.
docs/proposals/2026-05-18-cv-surgical-copilot-jeng/dist/
